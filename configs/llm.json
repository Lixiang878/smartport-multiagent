{
  "llm": {
    "api_base": "",
    "api_key": "",
    "model": "glm-4-flash",
    "timeout": 20
  },
  "_comment": "api_base/api_key 留空时读取环境变量 SMARTPORT_LLM_BASE_URL / SMARTPORT_LLM_API_KEY（兼容 OPENAI_API_KEY）。无密钥时系统自动以纯规则模式 100% 可运行。"
}
